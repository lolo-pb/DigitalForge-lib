import styles from "./Footer.module.css";

export function Footer() {
  return (
    <footer className={styles.footer}>
      <p>DigitalForge Playground</p>
      <p>Build, isolate, and learn from one interface at a time.</p>
    </footer>
  );
}

